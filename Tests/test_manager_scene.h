#pragma once

#include "Managers/manager_scene.h"
/*
========================================
 Одноразовые тесты (выполняются один раз)
 Диапазон: case -99 .. -1
========================================
-99: Инициализация тестового окружения
 - создание тестового окна/контекста
 - проверка валидности backends (input/render) в тестовом режиме

-98: Создание группы окон
 - создать N окон, проверить регистрацию и уникальные ID
 - закрыть все, убедиться в чистом завершении

-97: Создание/удаление одной сцены
 - создать сцену по минимальному конфигу
 - удалить сцену, проверить отсутствие «подвешенных» ссылок

-96: Создание нескольких сцен по конфигу
 - порядок сцен сохранён, активная = null (или по политике)

-95: Дубликаты имён/ID
 - попытка создать дубликат → ожидаемая политика (ошибка/перезапись/игнор)

-94: Ленивая инициализация (lazy)
 - сцена помечена lazy → OnCreate не вызывается до активации

-93: Жизненный цикл базовый
 - последовательность OnCreate → OnActivate → OnDeactivate → OnDestroy

-92: Ошибка в OnCreate
 - исключение/ошибка не оставляет сцену в реестре

-91: Переключение с историей
 - активировать A → B → C, закрыть C → авто-возврат на B

-90: Удаление активной сцены
 - удалить активную → переключение на предыдущую/дефолтную

-89: Сохранение/восстановление (cold start)
 - сохранить снапшоты всех сцен при завершении
 - при старте восстановить активную и состояние сцен

-88: Горячая перезагрузка конфига
 - заменить конфиг на лету → атомарное применение новых настроек

-87: Контракт с render-manager (однократная проверка)
 - при активации сцены вызывается ApplyRenderSettings(settings)
 - отписка при деактивации

-86: Контракт с input-manager (однократная проверка)
 - Set_ActiveScene(scene) вызывается при каждом переключении

-85: Многократное создание/закрытие окна
 - серия: создать → показать → закрыть (K раз) без утечек ресурсов



0: Финал одноразовых тестов
 - лог «one-time suite done», переход к case 0


========================================
 Маяк старта рантайм-проверок
========================================
 1: TEST_LOG::Info() << "Runtime checks started";


========================================
 Рантайм-тесты (Debug-only), с проваливанием
 Диапазон: case 1 .. 199 (и далее)
 Каждый кадр/тик вызывает следующий case без break
========================================
2: Актуальность активной сцены
 - активная сцена != null если в конфиге есть авто-активация
 - ссылка у input-manager совпадает с active_scene_

3: Инварианты регистра сцен
 - уникальность ID/имён; нет «дыр» в индексах; размеры контейнеров согласованы

4: Обработка событий клавиатуры
 - события идут только в active_scene (если не включён global capture)

5: Обработка событий мыши/геймпада
 - порядок событий не нарушается, дубликатов нет

6: Режим «input capture»
 - при активном capture события не «протекают» в другие подсистемы

7: Пауза/фоновые сцены
 - в паузе нет tick/update у active_scene (или минимальный), фоновые — согласно политике

8: Тайминг: variable timestep
 - дельта времени в допустимом диапазоне; нет дрейфа часов

9: Тайминг: fixed timestep
 - аккумулирование дельт; лимит catch-up; защита от spiral of death

10: Целевой FPS
 - если target_fps > 0 → средний fps стремится к цели, variance в пределах

11: VSync/лимит кадров
 - флаги применены в render-manager, нет конфликтов с target_fps

12: Качество/тесселяция (live)
 - применение пресетов качества уходит один раз при изменении

13: Нагрузочный поток событий
 - при N событий/кадр нет потери порядка и пропусков обработчиков

14: Сохранение состояния при переключении
 - snapshot активной сцены создаётся до деактивации, корректно применяется при возврате

15: Доступ к сценам по имени/ID
 - поиск O(1)/O(log N) по договору; не растёт сверх меры

16: Контракт render-manager (live)
 - ApplyRenderSettings/SetTargetFps не дергаются чаще 1/кадр при неизменных значениях

17: Контракт input-manager (live)
 - Set_ActiveScene вызывается строго при смене активной сцены

18: Диагностика/метрики
 - экспорта метрик fps/frame_time/num_scenes/active_id достаточно для профайлера

19: Потоки/синхронизация
 - публичные API, помеченные как thread-safe, выдерживают параллельные вызовы (smoke)

20: Deadlock-барьеры
 - последовательности switch→save→notify не блокируются (сторожевые таймауты)

21: Защита от рекурсивного переключения
 - попытка switch() внутри OnActivate не приводит к циклу

22: Обнаружение блокеров кадра
 - хук «frame watchdog»: предупреждение при frame_time > threshold_ms (Debug)

23: Проверка «живых» ресурсов (утечки, Debug)
 - счётчики сцен/окон/хэндлов не растут монотонно на горизонте K кадров

24: Проверка корректного разрушения объектов
 - падение «живых» объектов к базовой линии после серии переключений

25: Ограничение частоты логов
 - одинаковые варнинги агрегируются (no log-spam)

26: Масштаб (live)
 - при M=100+ сцен операции add/remove/switch укладываются в бюджет по времени

========================================
*/
namespace EDD::Tests {

class TestManagerScene {
 private:
  bool is_do_once = false;  // запуск тестов только один раз
  int test_case = -100;     // номер теста, который выполняется
  Managers::Scene *scene_manager_ = nullptr;

 public:
  void RunTests() {
    switch (test_case) {
      case -99: {
        [[fallthrough]];
      }
      case 0: {
        break;
      }
      case 1: {
        [[fallthrough]];
      }
      case 100: {
      }
    }
    //..
    is_do_once = true;
  }

 private:
};

}  // namespace EDD::Tests